<% if current_user.admin? %>
<li class="dropdown">
  <a href="#" class="dropdown-toggle" data-toggle="dropdown">
    管理者メニュー<b class="caret"></b>
  </a>
  <ul class="dropdown-menu">
    <li><%= link_to "タイヤ登録", new_item_path if current_user.admin? %></li>
    <li><%= link_to "カテゴリ登録", new_category_path if current_user.admin? %></li>
    <li class="divider"></li>
  </ul>
</li>
<% end %>
