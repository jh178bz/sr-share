<div id="favorite-<%= @item.id %>">
  <% if current_user.present? && current_user.favorite?(@item) %>
    <%= render 'users/unfavorite' %>
  <% else %>
    <%= render 'users/favorite' %>
  <% end %>
</div>
